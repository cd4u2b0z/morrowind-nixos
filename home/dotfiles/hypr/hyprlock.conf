# ╔═══════════════════════════════════════════════════════════════════╗
# ║  🔒 HYPRLOCK CONFIGURATION - NORD THEME                           ║
# ║  Polished Arctic-inspired lock screen                             ║
# ╚═══════════════════════════════════════════════════════════════════╝

# ═══════════════════════════════════════════════════════════════════
# 🎨 GENERAL CONFIGURATION
# ═══════════════════════════════════════════════════════════════════

general {
    hide_cursor = true
    ignore_empty_input = false
    immediate_render = false
    text_trim = true
    fail_timeout = 150
}

# ═══════════════════════════════════════════════════════════════════
# 🖼️ BACKGROUND CONFIGURATION
# ═══════════════════════════════════════════════════════════════════

background {
    monitor =
    path = /home/craig/Pictures/Wallpapers/girlinsnow.jpg
    color = rgba(46, 52, 64, 1.0)
    
    # Elegant blur effect
    blur_passes = 2
    blur_size = 5
    noise = 0.02
    contrast = 1.1
    brightness = 0.85
    vibrancy = 0.3
    vibrancy_darkness = 0.3
}

# ═══════════════════════════════════════════════════════════════════
# 👤 AVATAR IMAGE (Circular)
# ═══════════════════════════════════════════════════════════════════

image {
    monitor =
    path = /home/craig/.config/hypr/assets/avatar.jpg
    size = 140
    rounding = -1
    border_size = 4
    border_color = rgba(136, 192, 208, 0.9)
    position = 0, 120
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 6
    shadow_color = rgba(0, 0, 0, 0.7)
    shadow_boost = 1.2
}

# ═══════════════════════════════════════════════════════════════════
# 🕒 TIME DISPLAY
# ═══════════════════════════════════════════════════════════════════

label {
    monitor =
    text = cmd[update:1000] echo "$(date +'%H:%M')"
    color = rgba(236, 239, 244, 1.0)
    font_size = 120
    font_family = Inter Bold
    position = 0, 330
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 8
    shadow_color = rgba(0, 0, 0, 0.7)
    shadow_boost = 1.3
}

# ═══════════════════════════════════════════════════════════════════
# 📅 DATE DISPLAY
# ═══════════════════════════════════════════════════════════════════

label {
    monitor =
    text = cmd[update:60000] echo "$(date +'%A, %B %d')"
    color = rgba(136, 192, 208, 1.0)
    font_size = 20
    font_family = Inter Medium
    position = 0, 240
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgba(0, 0, 0, 0.5)
}

# ═══════════════════════════════════════════════════════════════════
# 🔐 PASSWORD INPUT FIELD
# ═══════════════════════════════════════════════════════════════════

input-field {
    monitor =
    size = 320, 55
    outline_thickness = 3
    dots_size = 0.25
    dots_spacing = 0.25
    dots_center = true
    dots_rounding = -1
    
    # Elegant glass effect
    outer_color = rgba(136, 192, 208, 0.7)
    inner_color = rgba(59, 66, 82, 0.85)
    font_color = rgba(236, 239, 244, 1.0)
    
    placeholder_text = <span foreground="##8fbcbb">󰌾  Enter Password</span>
    hide_input = false
    rounding = -1
    
    check_color = rgba(163, 190, 140, 1.0)
    fail_color = rgba(191, 97, 106, 1.0)
    fail_text = <span foreground="##bf616a">󰅜  Try Again</span>
    
    capslock_color = rgba(235, 203, 139, 1.0)
    numlock_color = -1
    bothlock_color = -1
    invert_numlock = false
    swap_font_color = false
    
    position = 0, -60
    halign = center
    valign = center
    
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(0, 0, 0, 0.5)
}

# ═══════════════════════════════════════════════════════════════════
# ⚡ STATUS INDICATORS
# ═══════════════════════════════════════════════════════════════════

# Caps Lock warning
label {
    monitor =
    text = cmd[update:500] if [[ $(cat /sys/class/leds/*/brightness 2>/dev/null | head -1) -gt 0 ]] || xset -q 2>/dev/null | grep -q "Caps Lock:   on"; then echo "󰪛 CAPS LOCK"; fi
    color = rgba(235, 203, 139, 1.0)
    font_size = 12
    font_family = Inter Bold
    position = 0, -130
    halign = center
    valign = center
}

# Battery indicator (laptops)
label {
    monitor =
    text = cmd[update:30000] cat /sys/class/power_supply/BAT*/capacity 2>/dev/null | head -1 | xargs -I{} echo "󰁹 {}%" || echo ""
    color = rgba(163, 190, 140, 0.9)
    font_size = 13
    font_family = Inter Medium
    position = 30, 30
    halign = left
    valign = bottom
}

# ═══════════════════════════════════════════════════════════════════
# 💻 SYSTEM INFO (Bottom)
# ═══════════════════════════════════════════════════════════════════

label {
    monitor =
    text = cmd[update:86400000] echo "$(hostnamectl hostname 2>/dev/null || uname -n)"
    color = rgba(76, 86, 106, 0.8)
    font_size = 11
    font_family = Inter Regular
    position = 0, 30
    halign = center
    valign = bottom
}
